spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reserve
    username: root
    password: 9999

  jpa:
    database: mysql
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false


#jwt:
#  key: Keys.secretKeyFor(SignatureAlgorithm.HS256)


#kakao:
#  key: KakaoAK YourKakaoRestAPIKey